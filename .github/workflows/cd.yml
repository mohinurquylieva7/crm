name: CD — Deploy

on:
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: cd-production
  cancel-in-progress: false

jobs:
  deploy:
    name: Deploy to production
    runs-on: ubuntu-latest
    timeout-minutes: 30
    environment:
      name: production
      url: https://crm-uz.duckdns.org

    steps:
      - uses: actions/checkout@v4

      - name: Setup SSH
        run: |
          mkdir -p ~/.ssh
          if [ -z "${{ secrets.SSH_PRIVATE_KEY }}" ]; then
            echo "ERROR: SSH_PRIVATE_KEY secret not set!"
            exit 1
          fi
          if [ -z "${{ secrets.SSH_HOST }}" ]; then
            echo "ERROR: SSH_HOST secret not set!"
            exit 1
          fi
          echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          ssh-keyscan -H "${{ secrets.SSH_HOST }}" >> ~/.ssh/known_hosts 2>/dev/null

      - name: Copy nginx config
        run: |
          ssh -i ~/.ssh/deploy_key -o StrictHostKeyChecking=no \
            "${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }}" \
            "sudo mkdir -p /opt/educrm && sudo chmod 755 /opt/educrm && sudo chown ${{ secrets.SSH_USER }}:${{ secrets.SSH_USER }} /opt/educrm"
          
          scp -i ~/.ssh/deploy_key -o StrictHostKeyChecking=no \
            nginx/nginx.conf \
            "${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }}:/opt/educrm/"

      - name: Deploy on server
        env:
          SSH_HOST: ${{ secrets.SSH_HOST }}
          SSH_USER: ${{ secrets.SSH_USER }}
        run: |
          GIT_TOKEN="${{ secrets.GHCR_TOKEN }}"
          CLONE_URL="https://mohinurquylieva7:${GIT_TOKEN}@github.com/mohinurquylieva7/crm.git"

          ssh -i ~/.ssh/deploy_key -o StrictHostKeyChecking=no \
            "${SSH_USER}@${SSH_HOST}" bash -s -- "$CLONE_URL" << 'ENDSSH'
            set -euo pipefail
            CLONE_URL="$1"
            REPO_DIR="/opt/educrm/repo"
            APP_DIR="/opt/educrm"

            echo "=== [1/5] Repo yangilash ==="
            if [ -d "$REPO_DIR/.git" ]; then
              cd "$REPO_DIR"
              git remote set-url origin "$CLONE_URL"
              git fetch origin main
              git reset --hard origin/main
            else
              git clone "$CLONE_URL" "$REPO_DIR"
            fi
            cd "$REPO_DIR"

            echo "=== [1b] Certbot sertifikat (xalos) ==="
            echo "Sertifikat olishi shart emas - skip"

            echo "=== [1c] .env ALLOWED_ORIGINS yangilash ==="
            sed -i 's|ALLOWED_ORIGINS=\["https://edu\.crm\.ozodbekme\.dev"\]|ALLOWED_ORIGINS=["https://crm-uz.duckdns.org"]|' /opt/educrm/.env 2>/dev/null || true
            grep ALLOWED_ORIGINS /opt/educrm/.env || true

            echo "=== [2/5] Docker build ==="
            echo "Checking .env file..."
            ls -la "$APP_DIR/.env" || echo "⚠️  .env file not found in $APP_DIR"
            cat "$APP_DIR/.env" | grep DB_PASSWORD || echo "⚠️  DB_PASSWORD not found in .env"
            DB_PASSWORD=$(grep DB_PASSWORD "$APP_DIR/.env" | cut -d= -f2)
            export DB_PASSWORD
            echo "DB_PASSWORD set to: ${DB_PASSWORD:0:5}***" # Show first 5 chars for safety
            sudo docker compose -f docker-compose.prod.yml build --parallel

            echo "=== [3/5] Konteynerlar ishga tushirish ==="
            sudo docker compose -f docker-compose.prod.yml down 2>/dev/null || true
            sudo docker compose -f docker-compose.prod.yml up -d --remove-orphans

            echo "=== [4/5] Migrations ==="
            sleep 8
            sudo docker exec educrm_api alembic upgrade head

            echo "=== [5/5] Health check ==="
            sleep 5
            for i in 1 2 3 4 5; do
              if curl -sf http://localhost:8000/health; then
                echo "Health OK (try ${i})"
                break
              fi
              echo "Waiting... (${i}/5)"
              sleep 8
            done

            sudo docker image prune -f --filter "until=24h" 2>/dev/null || true
            echo "Deploy muvaffaqiyatli: $(date)"
          ENDSSH

      - name: Summary
        if: always()
        run: |
          echo "## Deploy #${{ github.run_number }}" >> $GITHUB_STEP_SUMMARY
          echo "| | |" >> $GITHUB_STEP_SUMMARY
          echo "|---|---|" >> $GITHUB_STEP_SUMMARY
          echo "| Commit | \`${{ github.sha }}\` |" >> $GITHUB_STEP_SUMMARY
          echo "| URL | https://crm-uz.duckdns.org |" >> $GITHUB_STEP_SUMMARY
          echo "| API Docs | https://crm-uz.duckdns.org/docs |" >> $GITHUB_STEP_SUMMARY

  notify-failure:
    name: Notify on failure
    runs-on: ubuntu-latest
    needs: [deploy]
    if: failure()
    steps:
      - run: |
          echo "## Deploy muvaffaqiyatsiz!" >> $GITHUB_STEP_SUMMARY
          echo "**Commit:** ${{ github.sha }}" >> $GITHUB_STEP_SUMMARY
          echo "**Rollback:** \`git revert HEAD && git push\`" >> $GITHUB_STEP_SUMMARY
